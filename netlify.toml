[build]
  publish = "."
  command = "echo 'Static site - no build needed'"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[context.production.environment]
  # Set your backend API URL here when deploying to production
  API_URL = "https://your-backend-url.onrender.com"
